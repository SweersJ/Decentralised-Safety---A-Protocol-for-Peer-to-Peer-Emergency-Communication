# Version

This version is not based on a prior version. It creates a obsidian plugin to view quint files in obsidian.

# How to use

1. Put the folder `quint-highlighter-0.0.1` in the folder `.obsidian\plugins` and unzip the folder in that folder.