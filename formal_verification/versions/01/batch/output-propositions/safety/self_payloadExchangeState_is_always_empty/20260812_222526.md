## Information

### Name

self_payloadExchangeState_is_always_empty

### Type

invariant

### Logic family

- safety
- state predicate
- first-order logic

### Detailed logic family

- state predicate (safety)
- state predicate (universal)
- consistency/integrity invariant
- not CTL/LTL

### State variables

- payloadExchangeState

### Code reference

[batch.qnt](../../../batch.qnt#L942)

### Output reference

[Verification output](../../../output/safety/self_payloadExchangeState_is_always_empty/)

### Error notification

None

## Quint proposition snapshot

```quint
    val self_payloadExchangeState_is_always_empty = 
        NODES.forall(
            n => isDefaultPayloadExchangeState(n, n)
        )
```

## Formal verification formula

$$
\forall n \in \mathit{NODES}:\; \mathit{payloadExchangeState}[n][n]=\mathit{EMPTY\_PAYLOAD\_EXCHANGE\_STATE}
$$

## Formal verification textual message

Every self-directed exchange remains idle and unprocessed.

## Explanation

The diagonal lifecycle entry for each node must retain phase Idle with messagesStored and ackDetermined both false. This prevents actions from creating a lifecycle for self-communication. A counterexample pinpoints a transition that mutates a self-pair exchange entry.
