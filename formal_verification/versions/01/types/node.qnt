/// Define the protocol participant type used by version 01.
///
/// - @returns node the protocol participant type module
module node {
    /// Identify a protocol participant by name.
    ///
    /// - @returns Node the string node identifier
    type Node = str
}